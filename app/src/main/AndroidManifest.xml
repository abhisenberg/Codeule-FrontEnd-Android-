<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abheisenberg.cccliveschedule">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".IntroSliderScreen.IntroSliderActivity">
             <intent-filter>
                 <action android:name="android.intent.action.MAIN" />
                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            </intent-filter>
        </activity>

        <activity
            android:name=".Popups.HRContestDetails"
            android:theme="@style/PopupTheme" />
        <activity
            android:name=".Popups.HEContestDetails"
            android:theme="@style/PopupTheme" />

        <receiver
            android:name=".Notifications.NotificationPublisher"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".Popups.CCContestDetails"
            android:theme="@style/PopupTheme" />
        <activity android:name=".Settings.SettingsActivity" />

    </application>

</manifest>